namespace DarkLight.Events
{
    /// <summary>
    /// Functional groups are things which unify disparate user interface elements.  This allows us to link functionality between screens/windows.
    /// </summary>
    public enum LinkGroup
    {
        Shell,
        Symbol,
        Backtest,
    }
}