<Window x:Class="DarkLight.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:localViews="clr-namespace:DarkLight.Common.Views"
        Width="300"
        Height="400">

    <Grid Margin="3">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="DisplayName"
                   Grid.Row="0"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   FontSize="20" />
        <Button Grid.Row="0" Grid.Column="1" x:Name="ShowEventPublisher" Content="Events"/>
        <ContentControl Grid.Row="1" Grid.ColumnSpan="2" x:Name="ActiveItem"/>
        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Backtest" x:Name="NavigateToBacktestModule" Width="100"/>
            <Button Content="Optimize" x:Name="NavigateToOptimizationModule" Width="100"/>
            <Button Content="Trade" x:Name="NavigateToLiveTradingModule" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
